import {Layout} from "../../layouts";
import Position from "../position";

const Index = () => {
    return (
        <Layout>
            <Position/>
        </Layout>
    );
};

export default Index;
