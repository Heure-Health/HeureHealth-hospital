import React from "react";
import {Layout} from "../../../layouts";
import PositionCost from "./PositionCost";

const Cost = () => {
    return (
        <Layout>
            <PositionCost/>
        </Layout>
    );
};
export default Cost;
