import {Layout} from "../../layouts";
import Position from "../position/PositionMain"

const Index = () => {
    return (
        <Layout>
            <Position/>
        </Layout>
    );
};

export default Index;
